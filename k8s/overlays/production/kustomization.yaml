apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  # - external-secrets.yaml  # TODO: Re-enable after ExternalSecrets operator is deployed
  - ingress.yaml

patchesStrategicMerge:
  - configmap-patch.yaml
