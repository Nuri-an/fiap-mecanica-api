apiVersion: v1
kind: ConfigMap
metadata:
  name: fiap-mecanica-config
  namespace: fiap-mecanica
data:
  NODE_ENV: "production"
  PORT: "3000"
  API_PREFIX: "api/v1"
  JWT_EXPIRATION: "24h"
  CORS_ALLOWED_ORIGINS: "https://your-frontend-domain.com"
